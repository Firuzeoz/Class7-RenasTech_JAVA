package Day11_ForLoopCont;

public class Topic3_ForLoopsExp {
    public static void main(String[] args) {
        //write a code where you can find the even numbers between 10 to 120 (not included)
        int count=0;
        int count1=0;
        for (int i = 11; i <120 ; i+=2) {
            count++; //count=count+1
        }
        System.out.println("the count of even numbers is: "+count);
        //write a code where you can find the even numbers between 11 to 120 (not included)
        for (int i = 11; i <120 ; i+=2) {
            count1++; //count=count+1
        }
        System.out.println("the count of even numbers is: "+count1);







    }
}
